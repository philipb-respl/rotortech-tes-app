/** The four "+" registration-mark corners every blueprint-framed element
 *  (card, primary button, dialog) wears in the Industry design system.
 *  Pair with the `.blueprint` class on the parent. */
export function Corners() {
  return (
    <>
      <i className="corner tl" />
      <i className="corner tr" />
      <i className="corner bl" />
      <i className="corner br" />
    </>
  );
}
